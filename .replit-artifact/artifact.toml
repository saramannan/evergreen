kind = "web"
previewPath = "/"
title = "Daytime Enrichment Center"
version = "1.0.0"
id = "artifacts/enrichment-center"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23131

[services.development]
run = "pnpm --filter @workspace/enrichment-center run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/enrichment-center", "run", "build" ]
publicDir = "artifacts/enrichment-center/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23131"
BASE_PATH = "/"
